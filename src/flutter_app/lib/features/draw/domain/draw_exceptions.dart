/// No existe documento de asignación para el usuario actual (sorteo pendiente o datos no listos).
class MyAssignmentNotFoundException implements Exception {
  const MyAssignmentNotFoundException();
}
